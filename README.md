# X-Vault

Chrome extension that passively captures tweets as you browse Twitter/X. Store them locally, search across them, and export to LLMs.


https://github.com/user-attachments/assets/3dfe6c8c-c76e-44c9-bf0f-1961a0092439


## What it does

Browse any Twitter/X profile, tweets are captured automatically. Click the extension icon to see the dashboard. Use Ask LLM buttons to copy tweets + a prompt to your clipboard, then paste into Claude/ChatGPT.

### Capture Control
- **Profile pages** — always captured automatically
- **Individual tweets** — always captured when you open a specific tweet
- **Home feed** — disabled by default (toggle in popup settings)

## Install

1. Clone this repo
2. Open `chrome://extensions` in Chrome
3. Enable **Developer mode** (top right)
4. Click **Load unpacked** and select the repo folder
 
